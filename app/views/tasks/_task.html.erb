<!-- partial to display tasks -->
<div class="col-md-12">
	<div class="well">

		<h3><%= link_to task.description, task, remote: true, method: :delete, data: {confirm: 'Are you sure?'} %></h3>

		<p><%= task.deadline %></p>
		<p>
			<%= link_to "Edit", edit_task_path(task), class: "btn btn-info", remote: true %>
		</p>
		<p></p>
	</div>
</div>